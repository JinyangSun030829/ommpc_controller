# 绕圆前 minisnap 参数计算：需求、提示词与使用说明

## 明确后的任务提示词

> 在 ommpc_controller/traj 中实现 Python 3 离线工具 calc_minisnap_circle.py。
> 用户直接在.py文件开头设置圆半径（m）、圆周巡航段平均速度（m/s）、悬停油门百分比（21表示21%），然后手动运行，不传命令行参数。
> 根据当前两套控制器 YAML 的限幅和代码中的线性推力模型检查水平定高绕圆所需的向心加速度、jerk、倾角、推力和期望机体角速度。
> tracking和OMMPC必须分别给出结果、所需数值、原始限幅和预留裕度后的限幅；不能把一个控制器的限制套到另一个结果，也不能只输出第一个错误。
> 不把 minisnap 的 max_vel 直接当作巡航速度；复用当前 minisnap_core，在与 fly_cir.py setup=0 一致的标准航点模板上离线预演，校准巡航平均速度，并复核完整轨迹的 V/A/J 保守上界。
> 标准起点为圆周首点，34个圆周目标点，每圈8点，最后回圆心。平均速度指标取第二、第三圈，不包含第一圈起步、最后一圈及回圆心。
> 满足当前模型限制及显式裕度策略时，输出可复制到 minisnap_3D.launch 的 dev_order、max_vel、max_acc、max_jerk 和保证可复现所需的 compress_time 配置；同时报告预演平均/瞬时速度、半径误差、适用条件和未覆盖风险。
> 超出模型限幅或未通过当前方案检查时，报告不可实现及具体原因，不输出最终参数。
> 输入、配置、工具或求解异常必须报告无法判定，不能伪装成物理不可实现。
> 不修改当前 launch/YAML/fly_cir，不访问实时飞行状态，不发送轨迹、模式切换、解锁或降落命令。所有额外设置集中在脚本开头，并用中文说明。

## 实现规划及文件

1. `traj/calc_minisnap_circle.py`：三个输入、配置读取、模型限幅、速度校准、中文结果及 launch 片段。
2. `src/minisnap_circle_probe.cpp`：调用现有 `minisnap_core` 的离线桥接程序，不重复实现另一套 minimum-snap 求解器。
3. `tests/test_calc_minisnap_circle.py`：公式、单位、输入异常、裕度策略和校准逻辑测试。
   `tests/circle_calculator_ros_test.py`：在私有loopback ROS master上启动真实规划节点，验证预演速度及轨迹总时间一致，不启动飞行控制器，测试临时记录自动清理。
4. 使用 `/home/yundrone/Sunray/build_OMMPC.sh` 编译桥接程序，随后做真实求解器预演验证。

## 运行方式

先编辑 `traj/calc_minisnap_circle.py` 开头的三个用户设置：

```python
CIRCLE_RADIUS_M = 1.5            # 半径，m
TARGET_CRUISE_SPEED_MPS = 2.0    # 巡航段平均速度，m/s
HOVER_THRUST_PERCENT = 21.0      # 悬停油门百分比，21表示21%
```

保存后，在远程包目录中直接执行：

```bash
python3 traj/calc_minisnap_circle.py
```

上述设置表示半径1.5 m，巡航平均速度2 m/s，实测归一化悬停油门21%。**百分比不是0.21**，脚本不猜测百分比与小数之间的转换，也不接收命令行参数覆盖。

程序需要 Python 3、PyYAML 和本项目编译出的 `minisnap_circle_probe`；不需要启动 ROS master。直接执行脚本是推荐方式，开发环境下也注册了 `rosrun ommpc_controller calc_minisnap_circle.py`。

退出码：0＝标准模板参考轨迹通过模型及裕度检查；2＝模型/当前方案不满足要求；3＝无效输入、配置错误或无法判定。失败时不输出最终 launch 片段。

已验证示例（当前配置、标准起点）：半径1.5 m、目标巡航平均速度2 m/s、悬停油门21%，输出：

```xml
<arg name="dev_order" default="4"/>
<arg name="max_vel" default="2.16554019727"/>
<arg name="max_acc" default="4.59577305906"/>
<arg name="max_jerk" default="8.66958637421"/>
<!-- 此param替换规划节点内原有的同名param，不要放在launch全局 -->
<param name="planning/compress_time" value="false"/>
```

预演巡航平均速度为2.000000 m/s。私有ROS master上的真实规划节点验证了相同的平均速度和总时间22.931949923 s。示例只适用于本文列出的条件，配置变化后请重新运行，勿把示例当作所有半径/速度的通用设置。

## 三个输入能算出的量

水平定高理想圆，半径 r、速度 v、角速度 ω=v/r：

```text
向心加速度 a = v²/r
jerk 模长 j = v³/r²
单圈时间 T = 2πr/v
倾角 θ = atan(a/g)
所需推力加速度 F/m = sqrt(g²+a²)
归一化推力 u ≈ u_hover * sqrt(g²+a²)/g
```

这里沿用当前控制器的线性推力-加速度近似，质量在比值中消去，不要求用户再输入质量。它不是对真实电机、螺旋桨和电池推力曲线的测量。

固定 Euler 偏航、水平理想圆时，期望机体角速度的水平模长为 j/sqrt(g²+a²)，Z分量最大值为 a*j/(g*sqrt(g²+a²))。固定偏航不代表所有机体系角速度分量都是零。

对于离线求解出来的水平多项式轨迹，使用完整轨迹加速度、jerk 的保守上界 A、J，构造更保守的检查：水平机体角速度≤J/g，Z机体角速度≤(J/g)*A/sqrt(g²+A²)。这是期望姿态变化检查，不包括姿态反馈误差造成的额外角速度命令。

## 可行性范围与裕度

脚本每次读取 `config/traj_tracking_controller.yaml` 和 `config/params.yaml`，**独立检查两套控制器，不合并它们的推力限幅**：

- tracking 的标称加速度限幅、最大倾角、归一化推力上下限和竖直力上下限。
- OMMPC 的推力加速度和机体角速度限幅。
- OMMPC 当前 `send_cmd` 的归一化推力上下限0.04/0.90在脚本开头集中列出；若之后修改发送函数，需同步调整此处。

默认为模型上限预留20%：用于推力总量、水平力/倾角正切、标称加速度、OMMPC推力加速度及期望角速度。**这是工具的筛查策略，不是已测得的实际扰动裕度，也不是缩小圆半径或场地安全余量。**

理想圆本身超出某套控制器模型上限时，标记该套初筛未通过；原始上限允许但不满足20%裕度时注明是裕度策略拒绝。标准多项式校准结果未通过检查时，注明是当前模板/方案不通过，不宣称其他航点、起停设计也绝对不可行。

输出分为“理想圆初筛”“完整多项式预演”“最终结果”。每个阶段分别显示tracking和OMMPC的通过/未通过，并列出各项所需值、原始允许范围、裕度允许范围。只要有一个控制器初筛仍能通过，就继续预演，使另一个控制器初筛失败不会遮住它的完整检查结果；若两套初筛都失败，会明确标明未做完整预演。

仅当两套控制器初筛、完整预演及共同几何精度都通过，才输出共同适用的launch参数。若仅一套通过，仍分别报告，但不把它的可行性误写成“两套都可用”。求解或配置故障会将完整轨迹标为无法判定，而不是悄悄省略某套控制器。

## 为什么要调用现有求解器

`planning/vel` 是约束上限，不是要求每个时刻都以该速度运行。现有规划器会分配时间、按加速度/jerk 检查进行延时，还带有悬停起止边界。

工具先生成可行初值，再读取当前求解器输出的真实多项式，按数值积分计算巡航段的弧长/时间。若平均速度不达标，按时间缩放关系同时调整 V、A、J 上限（倍率分别为 s、s²、s³），重新调用求解器，最终要求平均速度误差不超过0.5%。打印值经过再次求解复核，不依赖隐藏精度。

V/A/J 检查使用项目求解器已有的整段 Bernstein 保守上界。巡航速度范围、弧长积分与半径误差使用每段128等分采样；它们是数值指标，**不是连续速度/几何误差的严格证明**。默认巡航采样半径误差不得超过设定半径的5%。

## 最终结果适用条件

- 固定高度，固定起飞偏航；无风等理想模型下分析，不包含反馈和DOB/UDE额外补偿需求。
- `fly_cir.py` 设置 `setup=0`、`radius=输入半径`，保留当前8点/圈、34个圆周目标点后回圆心的模板。
- 初始悬停位置必须在圆周首点 `(圆心x, 圆心y+radius, 固定高度)`。当前脚本圆心为(0,0)，则首点为(0,radius,z_start)。若从圆心或其他位置开始，入口段不同，不能把此结果当作已经验证的最终参数。
- 输出的 `dev_order=4`，并把规划节点内 `planning/compress_time` 改成 `false`，避免依赖计算机速度的软时间预算改变预演结果。它不关闭必要的超限延时调整。
- 保留预演所读取的其他规划参数；重新设置航点密度、圈数、起点或求解器参数后，应重新计算。
- 航点半径由 `fly_cir.py` 决定，不由 launch 中的 max_vel/max_acc/max_jerk 决定。有限阶多项式经过圆周航点，也不是数学上精确的圆。
- 输入悬停油门应来自当前载荷/电池条件下的测量，并与实际使用控制器的 `control/hover_thrust` 或 `hover_percentage` 保持一致；工具不会自动更改它们。

若节点因 `planning/max_result_age` 丢弃慢速求解结果，应另行分析运行电脑上的实际耗时，不自动放宽新鲜度门限。离线耗时不是在线耗时保证。

## 不包含的保证

这三个输入不能识别真实推力非线性、掉电、载荷变化、电机动态、最大可控角速度、风、闭环稳定性、定位质量，也不能确定圆心/高度与 `flight_box.yaml` 边界的关系。本工具不承担这些检查。

“可实现”严格限定为标准模板生成的**参考轨迹**通过所列模型和裕度检查，不是实际无人机一定达到目标速度或一定安全的承诺。先在仿真中检查实际生成的参考轨迹，再逐步验证真实跟踪。

相关背景：[PX4 控制器结构说明](https://docs.px4.io/main/en/flight_stack/controller_diagrams)中说明了加速度到姿态/推力转换及饱和处理；本工具的具体限幅和换算以本项目代码为准，而非用飞控默认参数替代现有控制器配置。
